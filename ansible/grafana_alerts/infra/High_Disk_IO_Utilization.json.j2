{
  "uid": "{{ alert_uid }}",
  "title": "High Disk IO Utilization",
  "folderUID": "{{ folder_uid }}",
  "ruleGroup": "{{ rulegroup }}",
  "condition": "C",
  "data": [
    {
      "refId": "A",
      "relativeTimeRange": {"from": 900, "to": 0},
      "datasourceUid": "{{ datasourceUid }}",
      "model": {
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "query": "SELECT derivative(mean(\"io_time\"), 1s) / 1000 * 100 AS \"io_util\"\nFROM \"diskio\"\nWHERE $timeFilter\nGROUP BY time(5m), host, project, zone\nfill(0)\n",
        "rawQuery": true,
        "refId": "A",
        "resultFormat": "time_series"
      }
    },
    {
      "refId": "B",
      "relativeTimeRange": {"from": 0, "to": 0},
      "datasourceUid": "__expr__",
      "model": {
        "expression": "A",
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "reducer": "last",
        "type": "reduce"
      }
    },
    {
      "refId": "C",
      "relativeTimeRange": {"from": 0, "to": 0},
      "datasourceUid": "__expr__",
      "model": {
        "conditions": [
          {
            "evaluator": { "params": [80], "type": "gt" },
            "operator": {"type": "and"},
            "query": { "params": ["C"] },
            "reducer": {"params": [], "type": "last"},
            "type": "query"
          }
        ],
        "expression": "B",
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "type": "threshold"
      }
    }
  ],
  "noDataState": "OK",
  "execErrState": "Error",
  "annotations": {
    "description": "🧐 **Why This Happens**\n- Heavy read/write operations on the disk.\n- Database workload or large file transfers.\n- Slow storage backend or hardware failure.\n- Too many concurrent processes generating I/O.\n\n🔍 **Impact**\n- Application latency and timeout issues.\n- Processes that rely on disk I/O might stall.\n- System-wide performance degradation.\n\n✅ **Immediate Actions for Ops**\n1. **Check real-time disk I/O stats:**\n   ```\n   iostat -x 1 5\n   sar -d 1 5\n   ```\n2. **Identify high I/O processes:**\n   ```\n   iotop -oPa\n   ```\n3. Reschedule heavy jobs (e.g., backups) if running.\n4. Investigate application generating heavy load.\n5. If issue persists:\n   - Run `smartctl -a /dev/\u003cdisk\u003e` to check disk health.\n   - Consider adding faster storage (SSD/NVMe) or increasing IOPS.\n   - Optimize application queries or enable caching.\n\n⚠ **Note:** This alert triggers if `io_util` (disk busy time %) stays above 90% for 2 minutes.",
    "summary": "Disk I/O utilization is critically high, which may impact system performance."
  },
  "labels": {
    "platform": "{{ project }}",
    "zone"    : "{{ zone }}",
    "severity": "CRITICAL",
        "type": "{{ (rulegroup | string).split('_')[0] }}"
  },
  "isPaused": false
}
