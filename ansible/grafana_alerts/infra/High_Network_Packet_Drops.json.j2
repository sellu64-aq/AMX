{
  "uid": "{{ alert_uid }}",
  "title": "High Network Interface Errors",
  "folderUID": "{{ folder_uid }}",
  "ruleGroup": "{{ rulegroup }}",
  "condition": "F",
  "data": [
    {
      "refId": "A",
      "relativeTimeRange": {"from": 900, "to": 0},
      "datasourceUid": "{{ datasourceUid }}",
      "model": {
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "query": "SELECT derivative(mean(\"drop_in\"), 1s) AS drop_in FROM \"net\" WHERE $timeFilter AND \"interface\" != 'lo' GROUP BY time(5m), project, zone, host, interface fill(null)",
        "rawQuery": true,
        "refId": "A",
        "resultFormat": "time_series"
      }
    },
    {
      "refId": "B",
      "relativeTimeRange": {"from": 900, "to": 0},
      "datasourceUid": "{{ datasourceUid }}",
      "model": {
        "datasource": {"type": "influxdb", "uid": "{{ datasourceUid }}"},
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "query": "SELECT derivative(mean(\"drop_out\"), 1s) AS drop_out FROM \"net\" WHERE $timeFilter AND \"interface\" != 'lo' GROUP BY time(5m), project, zone, host, interface fill(0)",
        "rawQuery": true,
        "refId": "B",
        "resultFormat": "time_series"
      }
    },
    {
      "refId": "C",
      "relativeTimeRange": {"from": 0, "to": 0},
      "datasourceUid": "__expr__",
      "model": {
        "conditions": [
          {
            "evaluator": { "params": [0, 0], "type": "gt" },
            "operator": {"type": "and"},
            "query": {"params": []},
            "reducer": {"params": [], "type": "avg"},
            "type": "query"
          }
        ],
        "datasource": {
          "name": "Expression",
          "type": "__expr__",
          "uid" : "__expr__"
        },
        "expression": "A",
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "reducer": "last",
        "refId": "C",
        "type": "reduce"
      }
    },
    {
      "refId": "D",
      "relativeTimeRange": {"from": 0, "to": 0},
      "datasourceUid": "__expr__",
      "model": {
        "conditions": [
          {
            "evaluator": { "params": [0, 0], "type": "gt" },
            "operator": {"type": "and"},
            "query": {"params": []},
            "reducer": {"params": [], "type": "avg"},
            "type": "query"
          }
        ],
        "datasource": {
          "name": "Expression",
          "type": "__expr__",
          "uid" : "__expr__"
        },
        "expression": "B",
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "reducer": "last",
        "refId": "D",
        "type": "reduce"
      }
    },
    {
      "refId": "E",
      "relativeTimeRange": {"from": 0, "to": 0},
      "datasourceUid": "__expr__",
      "model": {
        "conditions": [
          {
            "evaluator": { "params": [0, 0], "type": "gt" },
            "operator": {"type": "and"},
            "query": {"params": []},
            "reducer": {"params": [], "type": "avg"},
            "type": "query"
          }
        ],
        "datasource": {
          "name": "Expression",
          "type": "__expr__",
          "uid" : "__expr__"
        },
        "expression": "$C + $D",
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "refId": "E",
        "type": "math"
      }
    },
    {
      "refId": "F",
      "relativeTimeRange": {"from": 0, "to": 0},
      "datasourceUid": "__expr__",
      "model": {
        "conditions": [
          {
            "evaluator": { "params": [0, 0], "type": "gt" },
            "operator": {"type": "and"},
            "query": {"params": []},
            "reducer": {"params": [], "type": "avg"},
            "type": "query"
          }
        ],
        "datasource": {
          "name": "Expression",
          "type": "__expr__",
          "uid" : "__expr__"
        },
        "expression": "E",
        "intervalMs": 1000,
        "maxDataPoints": 43200,
        "refId": "F",
        "type": "threshold"
      }
    }
  ],
  "noDataState": "OK",
  "execErrState": "Error",
  "labels": {
    "platform": "{{ project }}",
    "zone"    : "{{ zone }}",
    "severity": "Major",
        "type": "{{ (rulegroup | string).split('_')[0] }}"
  },
  "annotations": {
    "description": "🧐 **Why This Happens**\n- Network congestion causing buffer overflows.\n- Faulty NIC or driver issues.\n- Misconfigured MTU (fragmentation issues).\n- Overloaded server unable to process packets fast enough.\n- Physical issues like bad cables or switch ports.\n\n🔍 **Impact**\n- Application latency and timeouts.\n- Packet retransmissions increasing CPU and network load.\n- Service degradation or failures under heavy traffic.\n\n✅ **Immediate Actions for Ops**\n1. **Check interface drop counters:**\n   ```\n   ip -s link show <interface>\n   ```\n2. Validate physical connections (replace cables if needed).\n3. **Check NIC queue and buffer settings:**\n   ```\n   ethtool -S <interface> | grep -E 'drop|err'\n   ```\n4. Monitor CPU and network interrupt load using:\n   ```\n   top, sar -n DEV 1\n   ```\n5. If MTU mismatch suspected, validate MTU settings on NIC and switch:\n   ```\n   ip link show <interface>\n   ```\n6. If persistent:\n   - Update NIC driver and firmware.\n   - Investigate application-level packet bursts or DoS traffic.\n\n⚠ **Note:** This alert triggers if combined `drop_in + drop_out` exceeds **10 packets/sec** for 5 minutes.",
    "summary": "High network packet drops detected on one or more interfaces."
  },
  "isPaused": false
}

